import { Meta, Canvas, Controls, } from "@storybook/addon-docs/blocks";
import * as IconStories from "./icon.stories";


<Meta
  of={ IconStories }
  title='Icon'
/>


# Icon

Icons are small symbols that represent actions and concepts, used to improve scannability and reinforce meaning alongside text.

<Canvas
  of={ IconStories.IconOverview }
  layout='padded'
  source={{
    code: `
      import { Icon, } from "@lucidjs/web";
      import { Search, } from "@lucidjs/icons";

      export const SearchIcon = () => {
        return (
          <Icon
            as={ Search }
            variant={{
              name: 'duotone',
              size: 'md',
              solid: false,
            }}
          />
        );
      }
    `,
  }}
/>
<Controls of={ IconStories.IconOverview }/>
